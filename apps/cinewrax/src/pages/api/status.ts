import { NextRequest, NextResponse } from "next/server";

export default async function handler(req: NextRequest, res: NextResponse) {
  // Get the sessionId, transcodingId from NextApiRequest
  // Get transcoding status
}
